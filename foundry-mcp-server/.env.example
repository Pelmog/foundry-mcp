# Foundry VTT connection config
FOUNDRY_HOST=localhost
FOUNDRY_PORT=30000
FOUNDRY_USER_ID=          # GM user ID (find with: game.users.find(u => u.isGM)?.id)
FOUNDRY_PASSWORD=          # GM password (empty if none set)
